#pragma once

// MT timing adjustments
#define TAPPING_TERM 250     // Increase from default 200ms
#define PERMISSIVE_HOLD      // Allow hold to activate with other keys
#define RETRO_TAPPING        // Send tap on release if no other keys pressed

// Optional: fine-tune behavior
#define QUICK_TAP_TERM 0        // Disable quick tap repeat
